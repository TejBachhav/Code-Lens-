# GET `/api/orders/{wildcard}`

> **Summary**: Retrieve order details

## Description
This endpoint retrieves detailed information about an order based on the provided wildcard parameter. It returns a text/html response containing the order data.

---

## Reference Docs

### Schema Table
The table below describes the input parameters and request body fields for this endpoint:

| Element Name | Type | Required | Description | Sample Input |
|--------------|------|----------|-------------|--------------|
| `wildcard (path)` | `string` | `Yes` | Parameter in path | `"string"` |

### Possible Request to Hit
Below is a sample request format you can use to test or call the endpoint:

```bash
curl -X GET 'http://example.com/api/orders/123'
```

### Sample Output
The following is a sample response payload returned by the endpoint:

#### Status `200` — OK (text/html)
```json
{}
```

### Test Cases
Below are the recommended test cases for this endpoint:

#### should return 200 for valid request
Verifies that the API returns a 200 status code when provided with a valid wildcard parameter.

- **Request**: `GET /api/orders/12345`
- **Expected Status**: `200`

#### should return 400 for missing wildcard parameter
Verifies that the API returns a 400 status code when no wildcard parameter is provided.

- **Request**: `GET /api/orders/`
- **Expected Status**: `400`

#### should return 401 for missing authentication
Verifies that the API returns a 401 status code when no authentication is provided.

- **Request**: `GET /api/orders/12345`
- **Expected Status**: `401`

#### should return 404 for non-existent order
Verifies that the API returns a 404 status code when the requested order does not exist.

- **Request**: `GET /api/orders/99999`
- **Expected Status**: `404`

#### should return 500 for internal server error
Verifies that the API returns a 500 status code when an unexpected error occurs.

- **Request**: `GET /api/orders/12345`
- **Expected Status**: `500`


---

## Metadata
- **Framework**: servlet
- **Language**: xml
- **Handler**: `OrderController` in class `com.example.controller.OrderController`
- **Source Code**: [`src/main/webapp/WEB-INF/web.xml`](../../../src/main/webapp/WEB-INF/web.xml#L1)

_Generated by CodeLens · 28/5/2026_
