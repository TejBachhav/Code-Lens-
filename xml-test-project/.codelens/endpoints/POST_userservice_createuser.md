# POST `/UserService/createUser`

> **Summary**: Creates a new user

## Description
This endpoint allows the creation of a new user by providing necessary details in the request body. It returns a response indicating the success or failure of the operation.

---

## Reference Docs

### Schema Table
The table below describes the input parameters and request body fields for this endpoint:

| Element Name | Type | Required | Description | Sample Input |
|--------------|------|----------|-------------|--------------|
| `body` | `object` | `Yes` | __UNRESOLVED__: WSDL input message: tns:CreateUserRequest | `{}` |

### Possible Request to Hit
Below is a sample request format you can use to test or call the endpoint:

```bash
curl -X POST /UserService/createUser -H "Content-Type: application/soap+xml" -d '<CreateUserRequest><username>newuser</username><password>pass123</password></CreateUserRequest>'
```

### Sample Output
The following is a sample response payload returned by the endpoint:

#### Status `200` — OK (application/soap+xml)
```json
{}
```

### Test Cases
Below are the recommended test cases for this endpoint:

#### should return 200 for valid request
Verifies that the API returns a 200 status code when provided with a valid CreateUserRequest.

- **Request**: `POST /UserService/createUser`
- **Expected Status**: `200`
- **Assertions**:
  - Response status code is 200
  - Response body contains a CreateUserResponse object

#### should return 400 for missing username
Verifies that the API returns a 400 status code when provided with a request missing the username field.

- **Request**: `POST /UserService/createUser`
- **Expected Status**: `400`
- **Assertions**:
  - Response status code is 400
  - Response body contains an error message indicating missing username

#### should return 400 for invalid email format
Verifies that the API returns a 400 status code when provided with a request containing an invalid email format.

- **Request**: `POST /UserService/createUser`
- **Expected Status**: `400`
- **Assertions**:
  - Response status code is 400
  - Response body contains an error message indicating invalid email format

#### should return 401 for missing authentication
Verifies that the API returns a 401 status code when provided with a request missing authentication credentials.

- **Request**: `POST /UserService/createUser`
- **Expected Status**: `401`
- **Assertions**:
  - Response status code is 401
  - Response body contains an error message indicating missing authentication

#### should return 500 for internal server error
Verifies that the API returns a 500 status code when provided with a request that causes an internal server error.

- **Request**: `POST /UserService/createUser`
- **Expected Status**: `500`
- **Assertions**:
  - Response status code is 500
  - Response body contains an error message indicating internal server error


---

## Metadata
- **Framework**: wsdl
- **Language**: xml
- **Handler**: `createUser` 
- **Source Code**: [`src/main/resources/wsdl/UserService.wsdl`](../../../src/main/resources/wsdl/UserService.wsdl#L1)

_Generated by CodeLens · 28/5/2026_
