# POST `/UserService/getUser`

> **Summary**: Retrieve user details

## Description
This endpoint retrieves detailed information about a specific user. It accepts a SOAP request with the GetUserRequest schema and returns a SOAP response with the GetUserResponse schema.

---

## Reference Docs

### Schema Table
The table below describes the input parameters and request body fields for this endpoint:

| Element Name | Type | Required | Description | Sample Input |
|--------------|------|----------|-------------|--------------|
| `body` | `object` | `Yes` | __UNRESOLVED__: WSDL input message: tns:GetUserRequest | `{}` |

### Possible Request to Hit
Below is a sample request format you can use to test or call the endpoint:

```bash
curl -X POST https://example.com/UserService/getUser -H "Content-Type: application/soap+xml" -d '<GetUserRequest xmlns="http://example.com"><UserID>123</UserID></GetUserRequest>'
```

### Sample Output
The following is a sample response payload returned by the endpoint:

#### Status `200` — OK (application/soap+xml)
```json
{}
```

### Test Cases
Below are the recommended test cases for this endpoint:

#### should return 200 for valid request
Verifies that the API returns a 200 status code and a valid response for a valid request.

- **Request**: `POST /UserService/getUser`
- **Expected Status**: `200`
- **Assertions**:
  - Response status code is 200
  - Response body contains user information

#### should return 400 for missing userId
Verifies that the API returns a 400 status code and an error message when the required &#x27;userId&#x27; parameter is missing.

- **Request**: `POST /UserService/getUser`
- **Expected Status**: `400`
- **Assertions**:
  - Response status code is 400
  - Response body contains an error message indicating missing &#x27;userId&#x27;

#### should return 401 for missing auth
Verifies that the API returns a 401 status code and an error message when authentication is required but not provided.

- **Request**: `POST /UserService/getUser`
- **Expected Status**: `401`
- **Assertions**:
  - Response status code is 401
  - Response body contains an error message indicating missing authentication

#### should return 404 for non-existent user
Verifies that the API returns a 404 status code and an error message when the requested user does not exist.

- **Request**: `POST /UserService/getUser`
- **Expected Status**: `404`
- **Assertions**:
  - Response status code is 404
  - Response body contains an error message indicating the user does not exist

#### should return 500 for internal server error
Verifies that the API returns a 500 status code and an error message when an unexpected error occurs on the server.

- **Request**: `POST /UserService/getUser`
- **Expected Status**: `500`
- **Assertions**:
  - Response status code is 500
  - Response body contains an error message indicating an internal server error


---

## Metadata
- **Framework**: wsdl
- **Language**: xml
- **Handler**: `getUser` 
- **Source Code**: [`src/main/resources/wsdl/UserService.wsdl`](../../../src/main/resources/wsdl/UserService.wsdl#L1)

_Generated by CodeLens · 28/5/2026_
