# GET `/api/users/{wildcard}`

> **Summary**: Retrieve user details

## Description
This endpoint retrieves detailed information about a specific user based on the provided wildcard in the path. The response is returned as HTML.

---

## Reference Docs

### Schema Table
The table below describes the input parameters and request body fields for this endpoint:

| Element Name | Type | Required | Description | Sample Input |
|--------------|------|----------|-------------|--------------|
| `wildcard (path)` | `string` | `Yes` | Parameter in path | `"string"` |

### Possible Request to Hit
Below is a sample request format you can use to test or call the endpoint:

```bash
curl -X GET 'http://example.com/api/users/123'
```

### Sample Output
The following is a sample response payload returned by the endpoint:

#### Status `200` — OK (text/html)
```json
{}
```

### Test Cases
Below are the recommended test cases for this endpoint:

#### should return 200 for valid request
Verifies that the API returns a 200 status code for a valid request with a wildcard parameter.

- **Request**: `GET /api/users/123`
- **Expected Status**: `200`

#### should return 400 for missing wildcard parameter
Verifies that the API returns a 400 status code when the required wildcard parameter is missing.

- **Request**: `GET /api/users/`
- **Expected Status**: `400`

#### should return 401 for missing authentication
Verifies that the API returns a 401 status code when authentication is required but not provided.

- **Request**: `GET /api/users/123`
- **Expected Status**: `401`

#### should return 404 for non-existent user
Verifies that the API returns a 404 status code when the requested user does not exist.

- **Request**: `GET /api/users/999`
- **Expected Status**: `404`

#### should return 500 for internal server error
Verifies that the API returns a 500 status code when an unexpected error occurs on the server.

- **Request**: `GET /api/users/123`
- **Expected Status**: `500`


---

## Metadata
- **Framework**: servlet
- **Language**: xml
- **Handler**: `UserController` in class `com.example.controller.UserController`
- **Source Code**: [`src/main/webapp/WEB-INF/web.xml`](../../../src/main/webapp/WEB-INF/web.xml#L1)

_Generated by CodeLens · 28/5/2026_
