# {{method}} `{{path}}`

> **Summary**: {{summary}}

{{#if description}}
## Description
{{description}}
{{/if}}

---

## Reference Docs

### Schema Table
The table below describes the input parameters and request body fields for this endpoint:

{{#if schemaElements}}
| Element Name | Type | Required | Description | Sample Input |
|--------------|------|----------|-------------|--------------|
{{#each schemaElements}}
| `{{name}}` | `{{type}}` | `{{required}}` | {{description}} | `{{{sampleInput}}}` |
{{/each}}
{{else}}
*No input parameters or request body fields are required for this endpoint.*
{{/if}}

### Possible Request to Hit
Below is a sample request format you can use to test or call the endpoint:

```bash
{{{sampleRequest}}}
```

### Sample Output
The following is a sample response payload returned by the endpoint:

{{#each responseOutputs}}
#### Status `{{statusCode}}` — {{statusDescription}} ({{contentType}})
```json
{{{sampleOutput}}}
```
{{/each}}

{{#if testCases}}
### Test Cases
Below are the recommended test cases for this endpoint:

{{#each testCases}}
#### {{name}}
{{description}}

- **Request**: `{{method}} {{path}}`
- **Expected Status**: `{{expectedStatus}}`
{{#if assertions}}
- **Assertions**:
{{#each assertions}}
  - {{this}}
{{/each}}
{{/if}}

{{/each}}
{{/if}}

---

## Metadata
- **Framework**: {{framework}}
- **Language**: {{language}}
- **Handler**: `{{handler.name}}` {{#if handler.className}}in class `{{handler.className}}`{{/if}}
- **Source Code**: [`{{sourceFile}}`](../../../{{sourceFile}}#L{{sourceLines.[0]}})

_Generated by CodeLens · {{generatedAt}}_
